describe('CreateCustomerService', () => {
    it('should be able to create a new customer', () => {
        expect(1).toBe(1);
    });

    it('should not be able to create a new customer with an existent email', () => {
        expect(1).toBe(1)
    });
});
